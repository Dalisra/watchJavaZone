applicationType: default
defaultPackage: backend
testFramework: junit
sourceLanguage: java
buildTool: gradle
features: [app-name, data, data-jdbc, flyway, gradle, http-client-test, java, java-application, jdbc-hikari, junit, logback, micronaut-aot, micronaut-build, micronaut-http-validation, netty-server, postgres, reactor, readme, serialization-jackson, shade, static-resources, test-resources, validation, yaml, yaml-build]
